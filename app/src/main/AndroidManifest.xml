<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.chatting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".setting.ProfilePictureChangeActivity"></activity>
        <activity android:name=".chat.swipe.DeletionDialog" />
        <activity android:name=".ranChat.RandomChattingRoomActivity" />
        <activity android:name=".AddFriend.ReceiveFriendRequestActivity" />
        <activity android:name=".chat.addChatRoom.AddChatActivity" />
        <activity android:name=".chat.ChatRoomActivity" />
        <activity android:name=".setting.StateMessageChange" />
        <activity android:name=".setting.ProfileManagement" />
        <activity android:name=".setting.ConfigActivity" />
        <activity android:name=".friend.FriendListActivity" />
        <activity android:name=".AddFriend.AddFriendActivity" />
        <activity android:name=".chat.ChatListActivity" />
        <activity android:name=".ranChat.EnterRandomChat" />
        <activity android:name=".login.SignUpActivity" />
        <activity android:name=".ranChat.RandomChatActivity" />
        <activity android:name=".login.Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>