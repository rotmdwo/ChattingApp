<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.chatting">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".chat.addChatRoom.AddChatActivity"></activity>
        <activity android:name=".chat.ChatRoomActivity" />
        <activity android:name=".StateMessageChange" />
        <activity android:name=".ProfileManagement" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".friend.FriendListActivity" />
        <activity android:name=".AddFriend.AddFriendActivity" />
        <activity android:name=".chat.ChatListActivity"></activity>
        <activity android:name=".login.SignUpActivity" />
        <activity android:name=".ranChat.RandomChatActivity" />
        <activity android:name=".login.Login" />
        <activity android:name=".ranChat.RandomChattingRoom" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>